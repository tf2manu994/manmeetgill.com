image: ruby:latest

pages:
  script:
  - gem install jekyll
  - gem install jekyll-redirect-from
  - gem install jekyll-sitemap
  - gem install html-proofer
  - jekyll build
  - ruby tests.rb
  artifacts:
    paths:
    - _site/
  only:
  - master